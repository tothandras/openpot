module op.user {
    'use strict';

    // register op.user module
    angular.module('op.user', [
        /* App */
        'op.common',
        // 'op.login',
        'op.user',
        /* Third party */
        'ui.router'
    ]);
}
