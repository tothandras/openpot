module op.common {
    'use strict';

    // register op.common module
    angular.module('op.common', [
        /* Core */
        /* App */
        /* Third party */
        'LocalStorageModule'
    ]);
}
