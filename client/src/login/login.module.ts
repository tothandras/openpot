module op.login {
    'use strict';

    // register op.login module
    angular.module('op.login', [
        /* Core */
        /* App */
        'op.common',
        /* Third party */
        'ui.router',
        'LocalStorageModule'
    ]);
}
