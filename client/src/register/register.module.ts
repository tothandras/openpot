module op.register {
    'use strict';

    // register op.login module
    angular.module('op.register', [
        /* Core */
        /* App */
        'op.common',
        /* Third party */
        'ui.router'
    ]);
}
